# Tick-Tock-Clock 開発計画書

小学2年生向け アナログ時計・時刻と時間の学習アプリ

| 項目 | 内容 |
|---|---|
| 版 | 0.3 |
| 作成日 | 2026-09-02 |
| 更新日 | 2026-09-02（連動デジタル時計と経過分カウンターを追加） |
| 対象 | 小学2年生（1年生の復習・3年生の先取りを含む） |
| リポジトリ | `Satoshi-Hiramatsu/Tick-Tock-Clock`（private、設定済み） |
| 公開ドメイン | `tick-tock-clock.molkz.com`（DNSは小文字で登録する） |
| 画面タイトル | 「とけいのれんしゅう」（子ども向け表示名。変更可） |
| 配信形態 | Cloudflare Workers + 静的アセット |
| 姉妹アプリ | 算数筆算プリントジェネレーター、漢字問題ジェネレーター |

---

## 0. 版0.1からの変更点

| 項目 | 決定 | 本書への反映 |
|---|---|---|
| アプリ名・ドメイン | `tick-tock-clock.molkz.com` | 第9.6章、第11章 Sprint 6 |
| 履歴保存（D1） | **未定** | 保存処理を `lib/storage.js` で抽象化し、localStorage 実装を先に作る。D1採用の判断期限を Sprint 4 終了時とした（第9.7章） |
| 24時間表記・秒針 | 残す | 発展オプションとして第3章・第6章に明記 |
| 音声読み上げ | **不要** | Web Speech API に関する記述をすべて削除 |
| デザイン方針 | **分かりやすさを重視** | 第4.1章にデザイン原則、色の意味の固定表、文字・操作対象の寸法を新設 |
| 回答方式 | 3方式とも実装 | 初期値を4択とし、設定で切替（第5.4章） |
| GitHubリポジトリ | 分離済み | 完了 |

### 版0.3の変更（2026-09-02）

解説アニメーションに、アナログ時計と連動する**デジタル時計**と、動いた分数を数え上げる**経過分カウンター**を追加した（第4.2章「時計の下に置く2つの表示」、第4.3章、第4.5章、第9.4章、第13章A）。「〜分前」でも同じ表示を逆向きに動かす。

### 版0.2の変更

版0.1からの主な追加は、色と寸法の確定（第4.1章）、教科書式の「くぎって考える」解説（第4.4章）、SVGの座標と扇形パスの確定（第9.3章）、モジュールの関数仕様（第9.4章）、問題生成アルゴリズム（第9.6章）、テストケース一覧（第9.9章）、Sprint 0・1の作業分解（第12章）である。

---

## 1. この文書の位置付け

本書は Tick-Tock-Clock の実行計画である。目的、学習内容、視覚化の設計、問題パターン、画面、印刷仕様、技術仕様、スプリント、受け入れ基準を一つにまとめ、Sprint 0 から実装に着手できる粒度まで具体化している。

Sprint 2 以降で仕様の変更が必要になった場合は本書を改版し、版数と変更点を第0章に追記する。

---

## 2. 目的と対象

### 目的

小学2年生が、「1時間 = 60分」「60分を超えたら時が1つ進む（繰り上がり）」「〜分前は針を戻す（繰り下がり）」という、言葉の説明では伝わりにくい概念を、**アナログ時計の針が実際に動き、動いた分が「量」として色と形で見える**ことで理解できるようにする。

### 主要対象

- 主利用者：小学2年生。時計は読めるが「〜分後」「〜分前」「何分間」の計算でつまずいている子。
- 支援者：家庭で一緒に画面を見ながら説明したい保護者、プリントを印刷してテストしたい保護者・先生。

### 解決する課題

| 課題 | 本アプリでの対応 |
|---|---|
| 「10分後」を数字の足し算としてしか捉えられず、針の動きと結び付かない | 答えを出す前に分針が実際に動き、動いた範囲を帯（扇形）で塗る |
| 60分で時が繰り上がることが分からない（3時50分の20分後を「3時70分」と答える） | 分針が12を通過した瞬間に時の数字が変わり、「4時になった」と示す |
| 80分など60分を超える量を「1時間20分」に分解できない | 60分ブロックと端数ブロックを積む「時間ブロック」、および教科書式の「くぎって考える」の2通りで分解を見せる |
| 「〜分前」で針をどちらに動かすか分からない | 反時計回りのアニメーションと、向き・色・矢印の3つで区別する |
| 家庭で反復練習させたいがプリント教材の準備が大変 | 難易度を指定してA4テストを生成・印刷。解答付き、シードで再印刷可能 |

### 学習体験の原則

1. 答えを見せる前に、必ず針が動く過程を見せる（「動き → 量 → 数字」の順）。
2. 動いた量は、時計の帯・数直線・時間ブロックで同じ色を使って表す。
3. 誤答を罰にせず、アニメーションで再生して納得させてから次へ進む。
4. 1問1画面。画面に置く情報のかたまりは3つまで。
5. 動くのは針と量の表現だけ。装飾のアニメーションは入れない。
6. 漢字は小2までの配当を基本とし、ふりがな表示を切り替えられる。
7. 順位付け、課金誘導、広告、子ども向け画面の外部リンクを置かない。

---

## 3. 学習内容の整理

| 学年 | 内容 | 本アプリでの扱い |
|---|---|---|
| 1年 | 何時、何時半、何時何分の読み取り | 復習として P1 で対応。難易度★1 |
| 2年 | 時刻と時間の区別、1日 = 24時間、午前・午後・正午、1時間 = 60分、時刻と時間の計算（〜分後・前、〜時間後、2つの時刻の間の時間） | **本アプリの中心**。P1〜P10 |
| 3年 | 秒、時間どうしの計算 | **発展オプション**。設定で有効化すると秒針表示と24時間表記が使える（第6章） |

用語は教科書に合わせ、時刻（ある一点）と時間（長さ）を区別する。子ども向け画面では「〜時〜分」「〜分間」と書き分ける。

---

## 4. 中核コンセプト：時間を「量」として見せる

### 4.1 デザイン原則と色・寸法の確定

方針は「分かりやすさを重視」。姉妹アプリのトークン構成（`tokens.css`）は流用するが、値は子どもの判読性に合わせて上書きする。

#### 色の意味（固定。全画面・印刷で共通）

学習用時計の慣例に合わせ、**時針と時の数字は赤、分針と分の数字は青**で固定する。

| 意味 | 色 | トークン名 |
|---|---|---|
| 時針・時の数字 | 赤 | `--c-hour` |
| 分針・分の数字 | 青 | `--c-minute` |
| すすむ（〜分後）の移動 | 青系の塗り | `--c-forward` |
| もどる（〜分前）の移動 | 橙系の塗り | `--c-backward` |
| 1時間のかたまり | 上記の濃い塗り | `--c-*-strong` |
| 端数の分 | 上記の薄い塗り | `--c-*-weak` |
| きりのいい時刻の区切り | 黄のマーカー | `--c-mark` |
| 正解 | 緑 | `--c-correct` |
| もういちど（誤答） | グレー | `--c-retry` |
| 文字盤・目盛り | 墨 | `--c-ink` |

誤答に赤と×を使わない。赤は時針の色として予約する。

色だけに頼らないため、すすむは「⟳」と実線の縁、もどるは「⟲」と破線の縁を併用する。

#### 寸法

| 対象 | 値 |
|---|---|
| 子ども向け画面の本文 | 18px 以上 |
| 問題文 | 24px 以上（スマートフォンでも 20px 以上） |
| 答え・時刻の大きい表示 | 40px 以上 |
| ボタンの最小高さ | 56px（タッチ対象は 56 × 56px 以上） |
| ボタン間の間隔 | 12px 以上 |
| コントラスト比 | 本文 4.5:1 以上、時計の針と数字 7:1 以上 |
| 時計の表示サイズ | 画面幅の 70%、最大 360px、最小 240px |

#### フォント

既定を **BIZ UDPGothic**（ユニバーサルデザインフォント）とする。設定で M PLUS 1p、Klee One に切り替えられる。数字の 1 と 7、6 と 0 が判別しやすいことを選定基準とする。

#### 画面の情報量

れんしゅう画面に置くかたまりは「問題文」「時計」「回答」の3つだけ。数直線と時間ブロックは解説画面に置き、出題中は表示しない。

### 4.2 視覚化の要素

「〜分後 / 〜分前」の解説で使う要素は次の通り。

| 要素 | 役割 | 表現 |
|---|---|---|
| **移動の帯** | 分針が動いた範囲を見せる | 文字盤の内側にドーナツ状の帯を描く。開始角から終了角まで塗る |
| **年輪（周回）** | 60分を超えた量を「1周 = 1時間」として見せる | 1周ごとに1つ内側の帯へ移る。最大3周（180分） |
| **時針の扇形** | 時針も連動して動くことを見せる | 中心付近の小さい扇形。時針は分針の 1/12 の速さ |
| **繰り上がり表示** | 12を通過した瞬間を示す | 12の位置が光り、時の数字が更新される。「4時になった」と短く表示 |
| **数直線** | 同じ量を直線として見せる | 60分ごとに区切った横棒。帯と同じ色で塗り、再生位置をマーカーで示す |
| **時間ブロック** | 「80分 = 60分 + 20分」の分解を見せる | 60分ブロックと端数ブロックを積む。長さは分数に比例 |
| **式** | 数字での確認 | 段階的に表示（第4.4章の2方式） |

#### 時計の下に置く2つの表示（アナログと連動）

針が動いている間、時計の直下に次の2つを横に並べ、針と同じ「経過分」から毎フレーム更新する。針・帯・デジタル時計・カウンターが必ず同じ瞬間を指すことが要件である。

| 表示 | 内容 | 例（3時10分から80分後の途中、経過55分の時点） |
|---|---|---|
| **デジタル時計** | 今この瞬間の時刻。時は赤、分は青で、針と同じ色 | `4時05分` |
| **経過分カウンター** | 動き始めてから動いた分数。方向の矢印と語を添える | `⟳ すすんだ時間 55分` |

デジタル時計の規則：

- 分は整数で表示し、針の位置に合わせて1分ずつ変わる（進む場合は切り捨て、戻る場合は切り上げ）。
- 12を通過して時が変わる瞬間に、時の数字を一度大きくして戻す（0.3秒）。分の桁は変えない。
- 表示形式は設定に従う（「4時05分」、午前午後あり、24時間表記オン時は「16:05」）。

経過分カウンターの規則：

- 進む場合は `⟳ すすんだ時間 N分`、戻る場合は `⟲ もどった時間 N分` と表示し、N は0から1ずつ増える。
- 5分ごと（設定で10分ごと）に数字を一度大きくして戻し、「5分、10分、15分…」と数えている感覚を作る。
- 60分を超えた瞬間から、下の行に `= 1時間20分` のように分解した形を併記する。分解は時間ブロックと同じ色で示す。
- 動きが止まった時点の値が答えの根拠になる。「すすんだ時間 80分 = 1時間20分」が残ったまま、答え「4時30分」を表示する。

帯の上には、10分ごとに `10` `20` `30` … の小さな目印を経過に合わせて置く（設定でオフにできる）。カウンターの数字と帯の目印が同じ値で対応し、「何分、何分」と数えながら量が増えていく様子を、数字と面の両方で読める。

#### 年輪の設計判断

帯の幅はすべて等しく（16単位）、周回ごとに内側へ移す。半径が違うので面積は一致しないが、「角度＝量」という読み取りは一貫する。面積としての量は数直線と時間ブロックが担当する。

| 周回 | 内半径 | 外半径 |
|---|---|---|
| 1周目（0〜60分） | 52 | 68 |
| 2周目（60〜120分） | 34 | 50 |
| 3周目（120〜180分） | 16 | 32 |

### 4.3 再生シナリオ：「3時10分の80分後は？」

| 手順 | 画面の変化 | デジタル時計 | 経過分カウンター | 見せたい概念 |
|---|---|---|---|---|
| 0 | 3時10分を表示。数直線は 0〜80分 の空の棒 | `3時10分` | `⟳ すすんだ時間 0分` | 出発点 |
| 1 | 分針が2（10分）から時計回りに動き、1周目の帯（薄青）が伸びる。数直線が同じ速さで塗られる | `3時11分` … `3時59分` と1分ずつ | `1分` … `49分`。5分ごとに数字が一度大きくなる | 動いた分 = 量 |
| 2 | 経過50分で分針が12を通過。12が光り、時の数字が 3 → 4 に変わる。時針が4に届く | `4時00分`。時の桁が一度大きくなる | `50分` | 12を通ると時が1つ進む |
| 3 | 経過60分で1周目の帯が閉じ、「1時間」ラベルが付く。2周目の帯（濃青）が内側で伸び始める | `4時10分` | `60分` に加えて下の行に `= 1時間` が現れる | 1周 = 60分 = 1時間 |
| 4 | 経過80分で分針が6（30分）に停止。数直線が満たされる | `4時30分` | `80分` `= 1時間20分` | 合計の量 |
| 5 | 時間ブロックに「1時間」1個 +「20分」1個が積まれる | `4時30分` | そのまま残る | 量の分解 |
| 6 | 「こたえ 4時30分」を表示。式を表示 | `4時30分`（答えと同じ値が並ぶ） | そのまま残る | 数字への変換 |

手順6では、デジタル時計の値と答えが一致していることを、同じ色・同じ書式で並べて見せる。「針が止まった場所 = デジタル時計 = 答え」の対応を作る。

再生は「ゆっくり / ふつう / いっき」の3速度、一時停止、スライダーによる手動スクラブに対応する。スクラブ中も帯・数直線・ブロックが連動する。

**注意すべき2つの瞬間**：12の通過（経過50分）と1周の完成（経過60分）は別の瞬間である。前者で時の数字が変わり、後者で「1時間」が確定する。この違いを混同させないよう、12の通過は文字盤上の光、1周の完成は帯のラベルと、表現を分ける。ここは子どもの観察で理解度を確認する（第14章）。

### 4.4 2通りの解説方式

解説画面で切り替えられる。既定は「くぎって考える」とする。教科書の指導方法に合わせるためである。

#### A. くぎって考える（既定）

きりのいい時刻で区切る、学校で教わる方法。

```
3時10分 ─(50分)→ 4時00分 ─(30分)→ 4時30分
         区切り①              区切り②
```

- まず次のちょうどの時刻（4時）まで何分かを求め、黄のマーカーで区切る。
- 残りを進める。
- 「50分 + 30分 = 80分」を式で確認する。

#### B. 1時間のかたまりで考える

量として分解する方法。

```
80分 = 60分 + 20分 = 1時間 + 20分
3時10分 + 1時間 = 4時10分
4時10分 + 20分  = 4時30分
```

- 時間ブロックで 60 と 20 に分ける。
- 先に時針を1つ進め、次に分針を20分進める。

Aは「12をまたぐ」感覚、Bは「1時間のかたまり」の感覚を育てる。両方を見せることで、繰り上がりの理解を2方向から支える。

### 4.5 「〜分前」の表現

- 分針は反時計回りに動き、帯は橙系で塗る。縁は破線、矢印は「⟲」。
- 12を逆向きに通過した瞬間に時の数字を1つ戻す。
- デジタル時計は `4時00分` → `3時59分` → … と1分ずつ戻る。時が戻る瞬間は進む場合と同じく時の桁を一度大きくする。
- 経過分カウンターは `⟲ もどった時間 N分` で、N は0から増える（戻った量なので数は増える）。60分を超えたら `= 1時間10分` を併記する。
- 数直線は現在位置を右端に置き、左に向かって塗る。
- 「4時の40分前」のように分が0から戻る場合は、区切り方式で「4時00分 ─(40分もどる)→ 3時20分」と示し、3時台に入ることを強調する。

### 4.6 「何分間？」（経過時間）の表現

- 開始時刻の針を薄く残し、終了時刻の針を濃く表示する。
- 開始から終了まで針が動き、その間を帯で塗る。
- 区切り方式では「8時40分 ─(20分)→ 9時00分 ─(20分)→ 9時20分、あわせて40分間」と示す。

---

## 5. 問題パターン

### 5.1 一覧

| ID | 名称 | 例題 | 難易度の軸 | 印刷 |
|---|---|---|---|---|
| P1 | 時計をよむ | 「何時何分ですか？」 | 分の刻み（30 / 15 / 5 / 1分） | ○ |
| P2 | 時計にかく | 「7時25分の針をかきましょう」 | 分の刻み | ○ |
| P3 | 〜分後（繰り上がりなし） | 「午後3時です。10分後は？」 | 移動量 5〜50分 | ○ |
| P4 | 〜分後（繰り上がりあり） | 「3時50分の20分後は？」「3時10分の80分後は？」 | 移動量上限 60 / 120 / 180分 | ○ |
| P5 | 〜分前 | 「4時です。40分前は？」 | 繰り下がりの有無、移動量 | ○ |
| P6 | 〜時間後 / 〜時間前 | 「9時の3時間後は？」 | 12をまたぐか、午前午後をまたぐか | ○ |
| P7 | 何分間？ | 「8時40分に出て9時20分に着いた。かかった時間は？」 | 60分をまたぐか | ○ |
| P8 | 午前・午後・24時間 | 「午前9時の5時間後は？」「1日は何時間？」 | 午前午後のまたぎ | ○ |
| P9 | 単位の変換 | 「1時間20分は何分？」「90分は何時間何分？」 | 数の大きさ | ○ |
| P10 | 文章題 | 「学校を8時50分に出て20分歩いた。着いたのは？」 | P3〜P7の組み合わせ | ○ |

### 5.2 難易度プリセット

| ★ | 分の刻み | 移動量上限 | 繰り上がり | 対象パターン |
|---|---|---|---|---|
| ★1 | 30 / 15分 | 30分 | なし | P1, P2, P3 |
| ★2 | 5分 | 60分 | あり | P1〜P5 |
| ★3 | 5分 | 120分 | あり | P1〜P8 |
| ★4（発展） | 1分 | 180分 | あり | P1〜P10 |

各軸（分の刻み、移動量上限、繰り上がりの有無、方向、午前午後表記）は個別にも調整できる。

### 5.3 誤答選択肢（4択）の生成規則

ランダムな数字は使わず、典型的な誤りから作る。誤答には内部タグを付け、Phase 2 の苦手分析に使う。

| タグ | 誤りの内容 | 例（3時50分の20分後 = 4時10分） |
|---|---|---|
| `no-carry` | 60を超えても繰り上げない | 3時70分 → 表示は「3時10分」 |
| `hour-only` | 時だけ進めて分を進め忘れる | 4時50分 |
| `reverse` | 向きを逆にする | 3時30分 |
| `hour-misread` | 時針を1つ手前に読む | 3時10分 |
| `swap` | 時と分を取り違える | 10時4分 |

4択は正解1つと、そのパターンで起こりやすいタグから3つを選ぶ。重複した値になる場合は次のタグに送る。選択肢の並び順はシャッフルし、正解の位置が偏らないようにする。

### 5.4 回答方式

3方式すべてを実装する。既定は4択、設定で切り替える。

| 方式 | 操作 | 主な用途 |
|---|---|---|
| **4択**（既定） | 選択肢をタップ | 導入。読む負担が最も軽い |
| **針を合わせる** | 分針をドラッグ。時針は連動 | P2、および答えを針で表現させたいとき |
| **数を入力** | 「時」「分」をタップ式テンキーで入力 | 仕上げ。自力で数を作らせる |

---

## 6. 機能一覧とモード

| モード | 利用者 | 内容 |
|---|---|---|
| **まなぶ** | 子ども・保護者 | 時計を自由に動かす。+5 / +10 / +30 / +60分、−5 / −10 / −30 / −60分のボタンと針のドラッグ。時計の下のデジタル時計と経過分カウンターが針に連動し、動かした量が帯・数直線・ブロックでも常に見える。保護者の説明用デモにも使う |
| **れんしゅう** | 子ども | パターンと難易度を選んで出題。回答 → 正誤 → 解説アニメーション → 次へ。10問で1セット |
| **プリント** | 保護者・先生 | パターン・難易度・問題数・枚数を選んでA4テストを生成、印刷 / PDF保存。解答ページ付き |
| **せってい** | 保護者 | 下表の設定項目 |
| **きろく**（Phase 2 候補） | 保護者 | 練習履歴、パターン別の正答率、苦手の優先出題 |

### 設定項目

| 項目 | 選択肢 | 既定 |
|---|---|---|
| ふりがな | 表示 / 非表示 | 表示 |
| 午前・午後の表記 | 表示 / 非表示 | 表示 |
| 分の目盛り数字（0〜55） | 表示 / 非表示 | 表示 |
| 解説の方式 | くぎって考える / 1時間のかたまり | くぎって考える |
| 回答方式 | 4択 / 針を合わせる / 数を入力 | 4択 |
| アニメーション速度 | ゆっくり / ふつう / いっき | ふつう |
| カウンターの節目 | 5分ごと / 10分ごと | 5分ごと |
| 帯の10分ごとの目印 | 表示 / 非表示 | 表示 |
| 動きをへらす | する / しない | しない（OS設定を初期値に反映） |
| フォント | BIZ UDPGothic / M PLUS 1p / Klee One | BIZ UDPGothic |
| **発展：秒針を表示** | する / しない | しない |
| **発展：24時間表記** | する / しない | しない |

発展の2項目は小2の範囲外のため既定でオフとし、設定画面では「はってん（3年生いじょう）」の見出しの下にまとめる。

---

## 7. 画面構成

| ID | 画面 | 必須要素 |
|---|---|---|
| S01 | ホーム | 4つの大きな入口（まなぶ / れんしゅう / プリント / せってい）、現在時刻の時計 |
| S02 | まなぶ | 時計、デジタル時計、経過分カウンター、+/−ボタン群、速度切替、リセット、数直線、時間ブロック |
| S03 | れんしゅう：設定 | パターン選択、★難易度、はじめるボタン |
| S04 | れんしゅう：出題 | 問題文、時計、デジタル時計（開始時刻のみ。動かない）、回答UI、ヒント |
| S05 | れんしゅう：解説 | 正誤、時計、デジタル時計、経過分カウンター、アニメーション再生（速度・一時停止・スクラブ）、式、次へ |
| S06 | れんしゅう：結果 | できた数、まちがえた問題の一覧（タップで解説再生）、もう一度 / ホーム |
| S07 | プリント：設定 | パターン複数選択、難易度、問題数、枚数、出力内容、なまえ・日付欄、シード |
| S08 | プリント：プレビュー | A4プレビュー、印刷 / PDF、再生成 |
| S09 | せってい | 第6章の設定項目 |

### 画面遷移

- 子ども：`S01 → S03 → S04 → S05 → S04 → … → S06 → S01`
- 保護者：`S01 → S07 → S08 → 印刷ダイアログ`

ハッシュルーティング（`#home` `#learn` `#practice` `#print` `#settings`）とし、問題はURLで再現できる（例：`#practice?p=P4&d=3&seed=1234`）。

### ヒントの段階

| 段階 | 内容 |
|---|---|
| 1回目 | 「どちらに動かす？」と矢印だけを表示 |
| 2回目 | 次のちょうどの時刻までを黄のマーカーで示す |
| 3回目 | 途中まで針を動かして止める |

ヒントを使っても正解扱いとし、結果画面には「ヒントをつかった」と記録するだけにする。

---

## 8. 印刷（A4テスト）仕様

### レイアウト

A4縦 210 × 297mm、余白 15mm、内寸 180 × 267mm。

| 領域 | 高さ |
|---|---|
| ヘッダ（タイトル、なまえ・日付・点数欄） | 25mm |
| 問題領域 | 242mm |

問題領域は2列（各87mm、間6mm）。

| 問題数 | 行数 | 1行の高さ | 時計の直径 |
|---|---|---|---|
| 6問 | 3 | 80mm | 50mm |
| 8問 | 4 | 60mm | 44mm |
| 10問 | 5 | 48mm | 36mm |

### その他の仕様

| 項目 | 内容 |
|---|---|
| CSS | `@page { size: A4 portrait; margin: 0 }`、`print-color-adjust: exact` |
| 問題の形 | P1：時計 +「（　）時（　）分」。P2：針なし文字盤 + 時刻。P3〜P7：時計 + 文章 + 答え欄。P8〜P10：文章 + 答え欄 |
| 解答ページ | 同レイアウト。答えを太字、移動の帯をグレー＋斜線パターンで印刷 |
| 再現性 | シード付き乱数で生成。シードをURLに含め、同じURLで同じプリントを再生成 |
| 印刷時の色 | 針は赤・青のまま印刷。モノクロ印刷でも判別できるよう、時針は太い実線、分針は細い実線とし、長さで区別する |
| 画面プレビュー | A4サイズの `.sheet` を実寸表示。スマートフォンでは `transform: scale()` で縮小し、印刷ボタンを画面下部に固定 |

時計は画面と同じSVG描画関数を使い、オプション（線を太く、帯をグレー、針なし）で出し分ける。

---

## 9. 技術仕様

### 9.1 方針

- **ビルドなし**：Vanilla JS（ES modules）+ SVG + CSS。フレームワーク、バンドラ、外部ライブラリを使わない。
- **静的優先**：初期リリースは静的アセットのみで動く。Worker は最初から置き、`/api/*` を後から足せるようにする。
- **純粋関数の中核**：時刻計算・角度計算・問題生成は副作用のない関数にし、`node --test` で検証する。
- **単一の状態から描画**：アニメーション中のすべての表現を「経過分」1つの値から導出する。

### 9.2 モジュール構成

```
ブラウザ
  index.html ── app.js（ハッシュルーター・画面状態）
      ├─ lib/time.js       時刻の加減・正規化・単位変換（純粋）
      ├─ lib/angles.js     針の角度、帯のパス、周回分割（純粋）
      ├─ lib/clock-svg.js  文字盤・針・帯の描画（SVG DOM）
      ├─ lib/animator.js   requestAnimationFrame による補間・速度・スクラブ
      ├─ lib/rng.js        シード付き乱数（純粋）
      ├─ lib/storage.js    設定と履歴の保存（アダプタ差し替え可）
      ├─ lib/problems/*.js パターン別の問題生成（純粋）
      ├─ lib/print.js      A4シートの組版
      └─ tokens.css / style.css / print.css
          │
          ▼
Cloudflare Workers（src/worker.js）
      ├─ ASSETS（public/ を配信）
      └─ /api/*（D1採用時のみ）
```

### 9.3 時計SVGの座標系

`viewBox="0 0 200 200"`、中心 C = (100, 100)。すべてこの座標で描き、表示サイズはCSSで変える。

角度は12時方向を 0°、時計回りを正とする。

```
θ_minute = m × 6°
θ_hour   = (h mod 12) × 30° + m × 0.5°
θ_second = s × 6°          （発展オプション）

点の座標： x = 100 + r·sin(θ),  y = 100 − r·cos(θ)   （θはラジアン）
```

#### 半径の割り当て

| 要素 | 半径 |
|---|---|
| 文字盤の縁 | 94 |
| 分の数字 0〜55（オプション） | 87 |
| 分目盛り（細） | 78 → 84 |
| 5分目盛り（太） | 74 → 84 |
| 時の数字 1〜12 | 64 |
| 移動の帯 1周目 | 52 → 68 |
| 移動の帯 2周目 | 34 → 50 |
| 移動の帯 3周目 | 16 → 32 |
| 時針の扇形 | 0 → 30 |
| 分針の長さ / 太さ | 78 / 4 |
| 時針の長さ / 太さ | 52 / 6 |
| 秒針の長さ / 太さ | 84 / 1.5 |
| 中心キャップ | 4 |

#### 帯（アニュラスセクター）のパス

```
M  P(θ1, ro)
A  ro ro 0 largeArc sweep  P(θ2, ro)
L  P(θ2, ri)
A  ri ri 0 largeArc 1−sweep  P(θ1, ri)
Z

largeArc = |θ2 − θ1| > 180° ? 1 : 0
sweep    = θ2 > θ1 ? 1 : 0      （すすむ = 1、もどる = 0）
```

360°ちょうどの帯は円弧1本で描けないため、`<circle>` に `r = (ri + ro) / 2`、`stroke-width = ro − ri`、`fill="none"` を指定して描く。

#### 針の回転

針は `transform="rotate(θ 100 100)"` の属性で回すこと。CSS の `transform` は Safari で原点の扱いが異なるため使わない。アニメーション中は角度属性だけを更新し、パスは再生成しない。

### 9.4 モジュールの関数仕様

#### lib/time.js

```
toMinutes({h, m})            → 0〜1439 の通算分
fromMinutes(t)               → {h, m}（tは自動で 0〜1439 に正規化）
addMinutes(time, delta)      → {time, dayShift}  dayShift は日をまたいだ回数
diffMinutes(from, to)        → 経過分（0〜1439、to が先なら翌日扱い）
splitAtHour(time, delta)     → [{from, to, len}, …]  ちょうどの時刻で区切る
splitByHour(delta)           → {hours, minutes}  例：80 → {1, 20}
crossings(time, delta)       → 12を通過する経過分の配列  例：(3:10, 80) → [50]
formatTime(time, opts)       → 「午後3時10分」「15:10」
```

#### lib/angles.js

```
minuteAngle(m) / hourAngle(h, m) / secondAngle(s)  → 度
point(theta, r)              → {x, y}
lapSegments(startMin, delta) → [{lap, from, to, len}, …]  周回ごとに分割
annularPath(theta1, theta2, ri, ro) → SVGパス文字列
```

#### lib/animator.js

```
create({ duration, onFrame, onEvent })
play() / pause() / seek(ratio) / setSpeed(name)
```

`onFrame(elapsedMinutes)` が毎フレーム呼ばれ、描画側はこの1値からすべてを導出する。`onEvent` は 12通過（`carry`）、1周完成（`lap`）、5分または10分の節目（`tick`）を通知する。

デジタル時計と経過分カウンターも同じ `elapsedMinutes` から導く。針は小数のまま滑らかに回し、数字は整数に丸めて1分ずつ変える。

```
sign      = delta >= 0 ? +1 : −1
shown     = sign > 0 ? floor(elapsed) : ceil(elapsed)   // 整数の経過分
digital   = addMinutes(start, sign × shown)             // デジタル時計の値
counter   = shown                                       // カウンターの値
breakdown = shown >= 60 ? splitByHour(shown) : null     // 「= 1時間20分」
```

`shown` の値が変わったフレームだけ DOM のテキストを更新する。毎フレーム書き換えると数字がちらつくためである。5分ごと（設定で10分ごと）の節目で `tick` を発火し、カウンターの数字を一度大きくする。

速度は ゆっくり = 10分/秒、ふつう = 30分/秒、いっき = 即時。

`prefers-reduced-motion: reduce` または設定「動きをへらす」のとき、連続補間をやめて10分ごとのコマ送りにする。

#### 実装上の注意（検証済み）

本書の座標式・帯のパス・周回分割は Node で計算を確認済みである。そのうえで、次の2点は実装時に誤りやすい。

**1. 負の剰余**。JavaScript の `%` は負の値に対して負を返す。`lapSegments(10, -80)` の2周目の開始分は `(10 - 60) % 60 = -50` となり、そのまま角度に使うと帯が反対側に描かれる。剰余は必ず正規化する。

```js
const mod = (n, m) => ((n % m) + m) % m;
```

**2. 開始分が0のときの12通過の非対称性**。前へ進む場合、3時00分から12を通過するのは経過60分の時点である（開始時点では通過していない）。一方、後ろへ戻る場合、4時00分は動き出した瞬間に3時59分になるため、通過は経過0の時点で起きる。`crossings()` はこの非対称を意図的に持つ。テストで固定し、対称に「直さない」こと。

### 9.5 状態管理

グローバルな可変状態を持たず、画面ごとに1つの状態オブジェクトを持って再描画する。

```
AppState { route, settings, session?, printJob? }
Session  { problems[], index, answers[], hintsUsed[] }
```

`settings` は変更のたびに `storage.saveSettings()` を呼ぶ。

### 9.6 問題生成

各パターンは `generate(rng, options) → Problem` の純粋関数。同じシードと同じオプションからは必ず同じ問題列が出る。

```js
// 例：P4（〜分後・繰り上がりあり）
function generateP4(rng, o) {
  const step = o.step;                        // 1 / 5 / 15 / 30
  const h = rng.int(1, 12);
  const m = rng.pick(multiplesOf(step, 0, 59));
  const maxDelta = o.maxDelta;                // 60 / 120 / 180
  let delta;
  do {
    delta = rng.pick(multiplesOf(step, step, maxDelta));
  } while (m + delta < 60);                   // 繰り上がりを必ず起こす
  const { time } = addMinutes({ h, m }, delta);
  return buildProblem('P4', { h, m }, delta, time, o);
}
```

生成後に `validate(problem)` で次を検査する。

- 時が 1〜12（24時間表記オフのとき）、分が 0〜59。
- 開始分・移動量が刻みの倍数。
- 移動量が上限以下。
- パターン固有の条件（P4は繰り上がりあり、P3は繰り上がりなし、など）。
- 同一セット内で開始時刻と移動量の組が重複しない。

`rng` は mulberry32 を使う。

```js
function mulberry32(a) {
  return function () {
    let t = (a += 0x6D2B79F5);
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
```

### 9.7 保存の抽象化（D1の判断を先送りするため）

履歴保存をD1で行うかは未定のため、保存処理を `lib/storage.js` の1インターフェースに集約し、実装を差し替えられるようにする。

```
storage.loadSettings()            → Settings
storage.saveSettings(settings)
storage.recordResult(result)      → void
storage.listHistory(limit)        → Result[]
storage.summarize()               → { byPattern, byTag }
```

Sprint 5 までは `LocalStorageAdapter` のみを実装する。D1を採用する場合は `ApiAdapter` を追加し、`app.js` の1行を変えるだけで切り替わるようにする。データ形式は両者で同一とする。

**判断期限：Sprint 4 終了時**。それまでに決まらなければ localStorage のまま初期リリースし、Phase 2 で移行する。

### 9.8 Cloudflare 構成

`wrangler.jsonc`（初期リリース時）：

```jsonc
{
  "name": "tick-tock-clock",
  "main": "src/worker.js",
  "routes": [
    { "pattern": "tick-tock-clock.molkz.com", "custom_domain": true }
  ],
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "./public",
    "binding": "ASSETS",
    "not_found_handling": "none",
    "run_worker_first": ["/api/*"]
  }
}
```

D1を採用する場合のみ `d1_databases` と `migrations_dir` を追加する。

PWA として `manifest.webmanifest` と `sw.js` を置き、画面資産をキャッシュしてオフラインでも全モードが動くようにする。

### 9.9 テスト戦略とテストケース

| 層 | 方法 |
|---|---|
| 単体 | `node --test`（`tests/*.test.mjs`） |
| UI | Playwright（evaluator エージェント） |
| 印刷 | Chrome の PDF出力を目視確認 |
| 手動 | 子どもによる利用観察 |

#### 必須の単体テストケース

**time.js**

| 入力 | 期待 |
|---|---|
| `addMinutes({h:3,m:10}, 80)` | `{h:4,m:30}` |
| `addMinutes({h:11,m:50}, 20)` | `{h:12,m:10}` |
| `addMinutes({h:12,m:0}, -1)` | `{h:11,m:59}` |
| `addMinutes({h:4,m:0}, -40)` | `{h:3,m:20}` |
| `addMinutes({h:23,m:30}, 60)` | `{h:0,m:30}`、`dayShift = 1` |
| `addMinutes({h:0,m:0}, -1)` | `{h:23,m:59}`、`dayShift = -1` |
| `diffMinutes({h:8,m:40},{h:9,m:20})` | `40` |
| `diffMinutes({h:23,m:50},{h:0,m:10})` | `20` |
| `splitAtHour({h:3,m:10}, 80)` | `[{len:50},{len:30}]` |
| `splitAtHour({h:3,m:0}, 30)` | `[{len:30}]`（区切り不要） |
| `splitByHour(80)` | `{hours:1, minutes:20}` |
| `splitByHour(60)` | `{hours:1, minutes:0}` |
| `crossings({h:3,m:10}, 80)` | `[50]` |
| `crossings({h:3,m:0}, 120)` | `[60, 120]` |
| `crossings({h:4,m:0}, -40)` | `[0]`（開始直後に逆通過） |

**angles.js**

| 入力 | 期待 |
|---|---|
| `minuteAngle(15)` | `90` |
| `hourAngle(3, 30)` | `105` |
| `hourAngle(12, 0)` | `0` |
| `lapSegments(10, 80)` | `[{lap:0,len:60},{lap:1,len:20}]` |
| `lapSegments(10, -80)` | `[{lap:0,len:-60},{lap:1,len:-20}]` |
| `lapSegments(0, 180)` | 3セグメント、各60 |
| `annularPath` の出力 | `NaN` を含まない |

**problems**

- 同じシードで2回生成した結果が完全に一致する。
- ★1〜★4の各プリセットで100問生成し、全問が `validate` を通る。
- P3で繰り上がりが1件も起きない。P4で全件繰り上がる。
- 4択の選択肢に重複がなく、正解が必ず1つ含まれる。
- 正解の位置が10問中で偏らない（各位置が0回にならない）。

#### UIテストの観点

各モードの遷移、解説アニメーション完了後の答え表示、針ドラッグの丸め、印刷プレビューのページ数、スマートフォン幅（375px）でのレイアウト崩れ。

### 9.10 ディレクトリ構成

```
Tick-Tock-Clock/
├── public/
│   ├── index.html
│   ├── app.js
│   ├── tokens.css / style.css / print.css
│   ├── lib/
│   │   ├── time.js
│   │   ├── angles.js
│   │   ├── clock-svg.js
│   │   ├── animator.js
│   │   ├── rng.js
│   │   ├── storage.js
│   │   ├── print.js
│   │   └── problems/
│   │       ├── index.js
│   │       ├── read.js          (P1, P2)
│   │       ├── after-before.js  (P3, P4, P5, P6)
│   │       ├── duration.js      (P7)
│   │       ├── ampm.js          (P8)
│   │       ├── units.js         (P9)
│   │       └── story.js         (P10)
│   ├── icons/
│   ├── manifest.webmanifest
│   └── sw.js
├── src/
│   └── worker.js
├── tests/
│   ├── time.test.mjs
│   ├── angles.test.mjs
│   └── problems.test.mjs
├── docs/
│   ├── USAGE.md
│   └── screenshots/
├── wrangler.jsonc
├── package.json
├── README.md
├── .gitignore / .gitattributes
└── 時計の読み方_開発計画書.md（本書）
```

---

## 10. データモデル

### Problem

```js
{
  id: "P4-1234-03",
  pattern: "P4",
  start: { h: 3, m: 10 },
  ampm: "pm" | "am" | null,
  delta: 80,                    // 分。負なら前。P6 は60の倍数
  end:   { h: 4, m: 30 },       // P7 では start と end が与えられ delta が答え
  answerType: "time" | "minutes" | "hours" | "ampm" | "draw",
  text: "3時10分です。80分後は何時何分ですか。",
  choices: [ { label: "4時30分", value: {...}, tag: null }, … ],
  difficulty: 3,
  options: { step: 5, showMinuteNumbers: true }
}
```

### Settings（localStorage キー `ttc.settings`）

```js
{
  furigana: true, ampm: true, minuteNumbers: true,
  explainMode: "split",          // "split" | "chunk"
  answerMode: "choice",          // "choice" | "hand" | "input"
  speed: "normal", reduceMotion: false,
  font: "udp",                   // "udp" | "mplus" | "klee"
  showSeconds: false, use24h: false
}
```

### Result（localStorage キー `ttc.history`、D1採用時は同形式）

```js
{
  at: 1756800000000, pattern: "P4", difficulty: 3, seed: 1234,
  correct: 8, total: 10,
  wrongTags: { "no-carry": 2 }
}
```

---

## 11. 文言の方針

- 小2までの配当漢字を基本とし、それ以外はひらがなにするか、ふりがなを付ける。
- 1文は20字以内。1問の文章は2文まで。
- 否定的な語（まちがい、ダメ、×）を使わず、「もういちど みてみよう」と書く。
- ボタンは動詞で始める（「はじめる」「つぎへ」「もういちど」「いんさつする」）。

| 場面 | 文言 |
|---|---|
| 正解 | できた！ |
| 誤答 | おしい。もういちど みてみよう |
| ヒント | ヒントを みる |
| 解説の再生 | うごきを みる |
| 結果 | 10もんちゅう 8もん できました |

---

## 12. 開発スプリント計画

| Sprint | ゴール | 完了条件 |
|---|---|---|
| **0** 足場 | ローカルで空の画面が動く | `npm start` で S01 が表示される |
| **1** 時計エンジン | 時計が描け、動く | 「3時10分 + 80分」が第4.3章の通りに再生される |
| **2** 量の表現と操作 | 数直線・時間ブロック・針ドラッグ・2方式の解説 | 受け入れ基準A を満たす |
| **3** れんしゅう | 出題と解説ができる | 10問セットを通しで解ける |
| **4** プリント | A4テストを印刷できる | 6 / 8 / 10問がA4 1枚に収まる。**D1採用の可否を判断** |
| **5** 残りパターンと設定 | 全パターン・設定・PWA | 受け入れ基準B〜D を満たす |
| **6** 公開 | 専用ドメインで公開 | `tick-tock-clock.molkz.com` で全モードが動く |
| **Phase 2** | 記録と苦手対策 | 別途計画 |

Sprint 1〜2 が価値の中核なので、ここで子どもに触ってもらい、視覚化の分かりやすさを確認してから Sprint 3 に進む。

### Sprint 0 の作業分解

1. `package.json`（`start` = `wrangler dev`、`test` = `node --test`）、`wrangler.jsonc`（第9.8章の内容、routes はコメントアウトで用意）。
2. `src/worker.js`：`/api/*` 以外を `env.ASSETS.fetch` に流すだけの最小実装。
3. `public/index.html`：4つの入口を持つ S01 と、空の画面コンテナ。
4. `public/tokens.css`：第4.1章の色・寸法・フォントを定義。姉妹アプリのトークンを土台に値を上書き。
5. `public/app.js`：ハッシュルーターと `AppState` の骨組み。未実装画面は「じゅんびちゅう」と表示。
6. `tests/` に空のテストファイルと、`npm test` が通ることの確認。
7. `README.md` の雛形（姉妹アプリの構成に合わせる）。

### Sprint 1 の作業分解

1. `lib/time.js` を実装し、第9.9章の time テストを通す。
2. `lib/angles.js` を実装し、angles テストを通す。
3. `lib/clock-svg.js`：文字盤（縁・目盛り・時の数字・分の数字）、針3本、中心キャップの描画。静的表示の確認。
4. 帯の描画（`annularPath` と360°の `<circle>` 分岐）、年輪3段。
5. `lib/animator.js`：補間、速度3段、一時停止、`carry` / `lap` イベント。
6. S02「まなぶ」：+/−ボタン8個、リセット。
7. デジタル時計と経過分カウンター（第9.4章の導出式）。整数化した値が変わったフレームだけ更新する。
8. 12通過時の演出（12が光る、時の数字が変わる、デジタル時計の時の桁が一度大きくなる）と1周完成時のラベル、5分ごとの `tick` によるカウンターの強調。
8. iPad Safari で針の回転と帯の描画を確認する。

---

## 13. 受け入れ基準

### A. まなぶ（視覚化）

- [ ] 任意の時刻から +5 / +10 / +30 / +60分、−5 / −10 / −30 / −60分に針が動き、帯が動いた範囲と一致する。
- [ ] 60分を超える移動で、1周ごとに1つ内側の帯へ移る（年輪）。
- [ ] 分針が12を通過した瞬間に12が光り、時の数字が変わる。
- [ ] 1周が完成した瞬間に帯へ「1時間」のラベルが付く。
- [ ] 数直線と時間ブロックが帯と同じ量・同じ色で表示され、スクラブに連動する。
- [ ] 「80分 = 1時間20分」の分解が時間ブロックと式の両方で表示される。
- [ ] 「くぎって考える」で、次のちょうどの時刻に黄のマーカーが出て2段階に分かれる。
- [ ] 時針が分針と連動して動く（3時30分で時針が3と4の中間にある）。
- [ ] デジタル時計が針に連動して1分ずつ変わり、針が止まった時点の値が答えと一致する。
- [ ] 時が変わる瞬間に、デジタル時計の時の桁が一度大きくなる。
- [ ] 経過分カウンターが0から1ずつ増え、5分（または10分）ごとに数字が一度大きくなる。
- [ ] 経過が60分を超えると、カウンターの下に「= 1時間20分」の形で分解が併記される。
- [ ] 「〜分前」でデジタル時計が1分ずつ戻り、カウンターが「もどった時間」として増える。
- [ ] スクラブ中も、針・帯・デジタル時計・カウンターが同じ瞬間を指す。
- [ ] 針をドラッグして時刻を合わせられ、分の刻み設定に丸められる。
- [ ] 「動きをへらす」でコマ送り表示に切り替わる。

### B. れんしゅう

- [ ] P1〜P10 の各パターンで、難易度の制約を満たす問題が生成される。
- [ ] 同じシードから同じ問題列が再現される。
- [ ] 4択の誤答が第5.3章のタグから作られ、重複しない。
- [ ] 正解の位置が偏らない。
- [ ] 3段階のヒントが順に出る。ヒント使用でも正解扱いになる。
- [ ] 誤答後に必ず解説アニメーションが再生される。
- [ ] 回答方式3つ（4択・針を合わせる・数を入力）がすべて動く。
- [ ] 結果画面から、まちがえた問題の解説を再生できる。

### C. プリント

- [ ] 6 / 8 / 10問がA4縦1枚に収まり、問題がページ境界で切れない。
- [ ] 問題ページと解答ページの番号・内容が対応する。
- [ ] 解答ページで移動の帯がグレー＋斜線で印刷される。
- [ ] 同じシードのURLを開くと同じプリントが再生成される。
- [ ] モノクロ印刷でも時針と分針を長さ・太さで区別できる。
- [ ] スマートフォンでプレビューが縮小表示され、印刷ボタンが画面下部に固定される。

### D. 共通・アクセシビリティ

- [ ] タッチ対象が 56 × 56px 以上、間隔が 12px 以上。
- [ ] 本文のコントラスト比 4.5:1 以上、針と数字 7:1 以上。
- [ ] 色だけに頼らず、向きが矢印と線種でも区別される。
- [ ] ふりがなの切替ができ、小2配当外の漢字にふりがなが付く。
- [ ] キーボードだけで主要操作ができる。
- [ ] 初回表示が1秒以内（画面資産の合計 300KB 以下、外部ライブラリなし）。
- [ ] オフラインで全モードが動く。
- [ ] 発展オプション（秒針・24時間表記）が既定でオフで、設定から有効化できる。

---

## 14. 子どもによる確認

Sprint 2 終了時に、小学2年生に「まなぶ」を触ってもらい、次を確認する。

1. 説明なしで +ボタンを押し、針が動くことに気づくか。
2. 「3時10分の80分後」を再生したあと、「何時何分になった？」に答えられるか。
3. 12の通過（時が変わる）と1周の完成（1時間たつ）を混同しないか。混同する場合は、1周のラベル表示を弱めるか、片方ずつ順に見せる方式に変更する。
4. 「くぎって考える」と「1時間のかたまり」のどちらが分かりやすいか。
5. 帯・数直線・時間ブロックを同時に出して情報が多すぎないか。多い場合は既定で数直線のみとし、時間ブロックを「くわしく」に隠す。

観察の記録は氏名を含めず、学年と気づいた点だけを残す。

---

## 15. リスクと対策

| リスク | 対策 |
|---|---|
| 12の通過と1周の完成を混同する | 表現を分ける（光 / ラベル）。第14章の観察で確認し、必要なら段階表示に変更 |
| 帯・数直線・ブロックを同時に出すと情報が多い | 出題中は時計のみ。解説画面でも既定は帯＋数直線とし、ブロックは「くわしく」で展開 |
| 3周（180分）を超えると年輪が潰れる | 移動量の上限を180分に固定。それ以上は P6 として時針中心に見せる |
| 針ドラッグがタッチで細かく操作しづらい | 刻み設定で丸める。ドラッグ中は針を太くし、時刻を大きく表示する |
| 印刷時に帯の色が抜ける | `print-color-adjust: exact`。解答ページの帯はグレー＋斜線パターン |
| Safari で SVG の `transform` 挙動が異なる | CSS ではなく属性で `rotate` を指定し、Sprint 1 の時点で iPad Safari を確認 |
| ビルドなし構成でモジュールが増えすぎる | 1ファイル200行を目安に分割。共通の描画は `clock-svg.js` に集約 |
| D1採用の判断が遅れて手戻りする | 保存処理を `storage.js` に集約済み。判断期限を Sprint 4 終了時と明示 |

---

## 16. 未決事項

| 項目 | 状態 | 判断期限 |
|---|---|---|
| 履歴保存にD1を使うか | 未定。localStorage 実装を先行 | Sprint 4 終了時 |
| 子ども向け表示名 | 仮に「とけいのれんしゅう」 | Sprint 6 着手前 |
| 数直線と時間ブロックの既定表示 | 両方表示で開始 | Sprint 2 の子ども観察後 |
| 解説方式の既定 | 「くぎって考える」 | Sprint 2 の子ども観察後 |
