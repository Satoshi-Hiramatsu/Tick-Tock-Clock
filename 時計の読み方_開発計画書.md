# 時計の読み方 学習アプリ 開発計画書

| 項目 | 内容 |
|---|---|
| 版 | 0.1（ドラフト） |
| 作成日 | 2026-09-02 |
| 対象 | 小学2年生（1年生の復習・3年生の先取りを含む） |
| 配信形態 | ブラウザアプリ（Cloudflare Workers + 静的アセット、専用ドメイン） |
| 姉妹アプリ | 算数筆算プリントジェネレーター（sansu-hissan.molkz.com）、漢字問題ジェネレーター |

---

## 1. この文書の位置付け

本書は「小学2年生がアナログ時計の読み方と時刻・時間の計算を、ブラウザ上でグラフィカルに学習でき、A4テストも印刷できるアプリ」を開発着手できる粒度に整理した実行計画である。目的、学習内容、中核となる視覚化の設計、問題パターン、画面、印刷仕様、技術構成、スプリント、受け入れ基準、未決事項を一つにまとめる。

各スプリントの詳細タスクは、着手時に「Sprint N 開発引き渡し」として別途切り出す。

---

## 2. 目的と対象

### 目的

小学2年生が、「1時間 = 60分」「60分を超えたら時が1つ進む（繰り上がり）」「〜分前は針を戻す（繰り下がり）」という、言葉の説明では伝わりにくい概念を、**アナログ時計の針が実際に動き、動いた分が「量」として色と形で見える**ことで理解できるようにする。

### 主要対象

- 主利用者：小学2年生。時計は読めるが「〜分後」「〜分前」「何分間」の計算でつまずいている子。
- 支援者：家庭で一緒に画面を見ながら説明したい保護者、プリントを印刷してテストしたい保護者・先生。

### 解決する課題

| 課題 | 本アプリでの対応 |
|---|---|
| 「10分後」を数字の足し算としてしか捉えられず、針の動きと結び付かない | 答えを出す前に分針が実際に動き、動いた範囲を扇形で塗る |
| 60分で時が繰り上がることが分からない（3時50分の20分後を「3時70分」と答える） | 分針が12を通過した瞬間に「+1時間」を示し、時針が進む様子を見せる。周回ごとに扇形の色を変える |
| 80分など60分を超える量を「1時間20分」に分解できない | 60分ブロックと端数ブロックを積み上げる「時間ブロック」と数直線で量を分解して見せる |
| 「〜分前」で針をどちらに動かすか分からない | 反時計回りのアニメーションと、向きを示す矢印・色で区別する |
| 家庭で反復練習させたいがプリント教材の準備が大変 | 難易度を指定してA4テストを生成・印刷。解答付き、再印刷可能 |

### 学習体験の原則

1. 答えを見せる前に、必ず針が動く過程を見せる（「動き → 量 → 数字」の順）。
2. 動いた量は、扇形・数直線・時間ブロックの3つの表現で一貫した色を使う。
3. 誤答を罰にせず、同じ問題をアニメーションで再生して納得させてから次へ進む。
4. 1問1画面、大きな操作対象（針・ボタン）で、タブレットのタッチ操作でも使える。
5. 漢字は小2までの配当漢字を基本とし、ふりがな表示を切り替えられる。
6. 順位付け、課金誘導、外部リンクを子ども向け画面に置かない。

---

## 3. 学習内容の整理（学習指導要領との対応）

| 学年 | 内容 | 本アプリでの扱い |
|---|---|---|
| 1年 | 何時、何時半、何時何分の読み取り | 復習として「よむ」パターン（P1）で対応。難易度★1 |
| 2年 | 時刻と時間、1日 = 24時間、午前・午後・正午、1時間 = 60分、時刻と時間の計算（〜分後・前、〜時間後、2つの時刻の間の時間） | **本アプリの中心**。P2〜P8 |
| 3年 | 秒、時間の計算（時間の足し算・引き算） | 発展として「秒針の表示」「時間同士の計算」をオプションで提供（Phase 2） |

用語は教科書に合わせ、「時こく（時刻）」と「時間（長さ）」を明確に区別して表記する。

---

## 4. 中核コンセプト：時間を「量」として見せる

### 4.1 視覚化の要素

すべての「〜分後 / 〜分前」問題で、次の要素を同時に表示する。

| 要素 | 役割 | 表現 |
|---|---|---|
| **分針の移動扇形** | 分針が動いた範囲を面積として見せる | 文字盤の外周寄りに、開始位置から終了位置までの扇形を塗る。後 = 青系、前 = 橙系 |
| **周回リング** | 60分を超えた量を「1周 = 1時間」として見せる | 1周目は薄い色のフルリング、2周目は少し内側に濃い色の扇形（年輪状に重ねる）。最大3周（180分）まで |
| **時針の移動扇形** | 分針と連動して時針も動くことを見せる | 文字盤の内側に小さい扇形。時針は分針の 1/12 の速さで動く |
| **繰り上がりバッジ** | 12を通過した瞬間を強調する | 分針が12を通過した瞬間に「+1時間」が時計の上にポップし、時の数字が更新される。前の場合は「−1時間」 |
| **数直線** | 時計の外で、同じ量を直線として見せる | 0分から目標分までを60分ごとに区切った横棒。扇形と同じ色で塗り、再生位置をマーカーで示す |
| **時間ブロック** | 「80分 = 60分 + 20分 = 1時間20分」の分解を見せる | 60分ブロック（横長）と端数ブロックを積む。ブロックの長さは分数に比例する |
| **式の表示** | 数字での確認 | 「3時10分 → 80分後 → 4時30分」と、「10分 + 80分 = 90分 = 1時間30分」を段階的に表示 |

色だけに頼らず、後は「→（時計回り矢印）」、前は「←（反時計回り矢印）」の記号と、扇形の縁取りパターン（実線 / 破線）でも区別する。

### 4.2 再生シナリオ例：「3時10分の80分後は？」

| 手順 | 画面の変化 | 見せたい概念 |
|---|---|---|
| 0 | 3時10分を表示。数直線は 0〜80分 の空の棒。時間ブロックは空 | 出発点 |
| 1 | 分針が2（10分）から時計回りに動き始め、扇形（薄青）が伸びる。数直線が同じ速さで塗られる | 動いた分 = 量 |
| 2 | 分針が12を通過（50分経過）。「+1時間」バッジ、時の数字が 3 → 4 に変わる。時針が4に届く | 12を通ると時が1つ進む |
| 3 | 1周目の扇形はフルリングとして残り、2周目の扇形（濃青）が内側に伸び始める | 1周 = 60分 = 1時間 |
| 4 | 分針が6（30分）で停止。数直線は80分で満たされる | 合計の量 |
| 5 | 時間ブロックに「60分（1時間）」1個 + 「20分」1個が積まれ、「80分 = 1時間20分」と表示 | 量の分解 |
| 6 | 「答え：4時30分」を表示。式「3時10分 + 1時間20分 = 4時30分」 | 数字への変換 |

再生は「ゆっくり / ふつう / いっき」の速度、一時停止、スライダーによる手動スクラブに対応する。スクラブ中も扇形・数直線・ブロックが連動する。

### 4.3 「〜分前」の表現

- 分針は反時計回りに動き、扇形は橙系で反時計回りに塗る。
- 12を逆向きに通過した瞬間に「−1時間」バッジを表示し、時の数字を戻す。
- 数直線は現在位置を右端に置き、左に向かって塗る。
- 「4時の40分前」のように、分が0の時刻から戻る問題では、12から逆回りに動くので「3時60分 = 4時」の関係を強調する。

### 4.4 「何分間？」（2つの時刻の間）の表現

- 開始時刻と終了時刻の両方の針を薄く表示し、その間の扇形を塗る。
- 針は開始位置から終了位置まで動き、経過分を数直線と時間ブロックで示す。
- 60分を超える場合は周回リングと同じ表現を使う。

---

## 5. 問題パターン

| ID | 名称 | 例題 | 難易度の軸 | 視覚化 | 印刷 |
|---|---|---|---|---|---|
| P1 | 時計をよむ | 「何時何分ですか？」（時計を見て答える） | 分の刻み（30 / 15 / 5 / 1分） | 針の位置を強調、分の目盛り数字（0〜55）表示切替 | ○ |
| P2 | 時計にかく | 「7時25分の針をかきましょう」 | 分の刻み | 画面では針をドラッグ。印刷では針なし文字盤 | ○ |
| P3 | 〜分後（繰り上がりなし） | 「午後3時です。10分後は？」 | 移動量（5〜50分） | 扇形 + 数直線 | ○ |
| P4 | 〜分後（繰り上がりあり） | 「3時50分です。20分後は？」「3時10分の80分後は？」 | 移動量上限（60 / 120 / 180分） | 扇形 + 周回リング + 繰り上がりバッジ + 時間ブロック | ○ |
| P5 | 〜分前 | 「4時です。40分前は？」 | 繰り下がりの有無、移動量 | 反時計回り扇形 + 「−1時間」 | ○ |
| P6 | 〜時間後 / 〜時間前 | 「9時の3時間後は？」 | 12を跨ぐか、午前午後を跨ぐか | 時針の扇形を主役にし、分針は周回のみ | ○ |
| P7 | 何分間？（経過時間） | 「8時40分に出て、9時20分に着きました。かかった時間は？」 | 60分を跨ぐか | 2つの針 + 間の扇形 | ○ |
| P8 | 午前・午後・正午・24時間 | 「午前9時の5時間後は午前・午後どちらの何時？」「1日は何時間？」 | 午前午後の跨ぎ | 24時間の帯（午前 = 明るい色、午後 = 暗い色）と時計を並べる | ○ |
| P9 | 単位の変換 | 「1時間20分は何分？」「90分は何時間何分？」 | 数の大きさ | 時間ブロックのみ | ○ |
| P10 | 文章題 | 「学校を8時50分に出て、20分歩きました。着いたのは？」 | P3〜P7の組み合わせ | 該当パターンと同じ | ○ |

### 難易度プリセット

| ★ | 内容 |
|---|---|
| ★1 | 30分・15分・5分刻み、繰り上がりなし、移動量30分以内 |
| ★2 | 5分刻み、繰り上がりあり、移動量60分以内、「〜分前」あり |
| ★3 | 1分刻み、移動量120分以内、経過時間、午前午後の跨ぎ |
| ★4（発展） | 移動量180分以内、単位変換、文章題、時間同士の計算 |

各軸（分の刻み、移動量上限、繰り上がりの有無、方向、午前午後表記）は個別にも調整できる。

### 回答方式（れんしゅうモード）

- **4択**：時刻の選択肢（誤答は「繰り上がりを忘れた」「時針を読み間違えた」など典型的な誤りから生成する）。
- **針を合わせる**：答えの時刻になるよう分針をドラッグする。時針は連動して動く。
- **数を入力**：「時」「分」をタップ式のテンキーで入力する。

---

## 6. 機能一覧とモード

| モード | 利用者 | 内容 |
|---|---|---|
| **まなぶ**（自由操作） | 子ども・保護者 | 時計を自由に動かす。「+5分」「+10分」「+1時間」「−10分」ボタン、針のドラッグ。動かした量が常に扇形・数直線・ブロックで表示される。説明用のデモとしても使える |
| **れんしゅう** | 子ども | パターンと難易度を選んで出題。回答 → 正誤 → アニメーションによる解説 → 次へ。10問で1セット、最後に「できた数」と「もう一度見たい問題」を表示 |
| **プリント** | 保護者・先生 | パターン・難易度・問題数・枚数を選んでA4テストを生成。プレビュー、印刷 / PDF保存、解答ページ |
| **せってい** | 保護者・先生 | ふりがな、午前午後表記、分の目盛り数字、アニメーション速度、動きの抑制、フォント |
| **きろく**（Phase 2） | 保護者 | 練習の履歴、パターン別の正答率、苦手パターンの優先出題（D1に匿名IDで保存） |

---

## 7. 画面構成

| ID | 画面 | 主利用者 | 必須要素 |
|---|---|---|---|
| S01 | ホーム | 子ども | 大きな4つの入口（まなぶ / れんしゅう / プリント / せってい）、現在時刻の時計 |
| S02 | まなぶ | 子ども・保護者 | 大きな時計（SVG）、時刻表示、+/−ボタン群、速度切替、リセット、数直線、時間ブロック |
| S03 | れんしゅう：設定 | 子ども・保護者 | パターン選択（絵付き）、★難易度、回答方式、はじめるボタン |
| S04 | れんしゅう：出題 | 子ども | 問題文（読み上げボタン）、時計、回答UI、ヒント（針を少しだけ動かす） |
| S05 | れんしゅう：解説 | 子ども | 正誤表示、アニメーション再生（速度・一時停止・スクラブ）、式、次へ |
| S06 | れんしゅう：結果 | 子ども | できた数、間違えた問題の一覧（タップで解説を再生）、もう一度 / ホーム |
| S07 | プリント：設定 | 保護者・先生 | パターン複数選択、難易度、問題数（6 / 8 / 10問）、枚数（1〜10）、出力（問題のみ / 解答のみ / 問題＋解答）、なまえ・日付欄、シード表示 |
| S08 | プリント：プレビュー | 保護者・先生 | A4プレビュー（スマホでは自動縮小）、印刷 / PDFボタン、再生成 |
| S09 | せってい | 保護者 | 第6章の設定項目 |

### 主要画面遷移

- 子ども：`S01 → S03 → S04 → S05 → S04 → … → S06 → S01`
- 保護者：`S01 → S07 → S08 → 印刷ダイアログ`
- ハッシュルーティング（`#learn` `#practice` `#print` `#settings`）とし、問題はURLクエリで再現できる（例：`#practice?p=P4&seed=1234&n=3`）。

---

## 8. 印刷（A4テスト）仕様

| 項目 | 仕様 |
|---|---|
| 用紙 | A4縦（210 × 297mm）、余白 15mm、`@page { size: A4 portrait; margin: 0 }` |
| ヘッダ | タイトル、なまえ・日付・点数欄（任意表示） |
| レイアウト | 2列 × 3行（6問）、2列 × 4行（8問）、2列 × 5行（10問）。時計の直径は 6問で 50mm、10問で 38mm |
| 問題の種類 | P1：時計 + 「（　）時（　）分」。P2：針なし文字盤 + 時刻。P3〜P7：時計 + 文章 + 答え欄。P8〜P10：文章 + 答え欄（時計は任意） |
| 解答ページ | 同じレイアウトで、答えを赤字、動いた扇形を薄いグレーで印刷（印刷時に `print-color-adjust: exact`） |
| 再現性 | シード付き乱数で生成し、シードをURLに含める。同じURLで同じプリントを再印刷できる |
| フォント | 姉妹アプリと同じ切替（Klee One / BIZ UDPGothic / M PLUS 1p）。数字は判読性を優先 |
| 画面プレビュー | A4サイズの `.sheet` をそのまま表示し、スマホでは `transform: scale()` で縮小 |

時計は画面と同じSVG描画関数を使い、印刷用のオプション（線を太く、扇形をグレー、針なし）で出し分ける。

---

## 9. 技術構成

### 9.1 方針

- **軽量・ビルドなし**：Vanilla JS（ES modules）+ SVG + CSS。フレームワーク、バンドラは使わない（算数筆算と同じ構成）。
- **静的優先**：初期リリースは静的アセットのみで動く。Worker は最初から置き、`/api/*` を Phase 2 で追加できるようにする。
- **姉妹アプリと統一**：デザイントークン（tokens.css）、印刷CSS、PWA、フォント切替を流用する。
- **純粋関数の中核**：時刻計算・角度計算・問題生成を副作用のない関数にし、Node の `node --test` で検証する。

### 9.2 構成図

```
ブラウザ
  index.html ── app.js（ハッシュルーター・状態）
      ├─ lib/time.js        時刻の加減・正規化・単位変換（純粋関数）
      ├─ lib/angles.js      針の角度、扇形パス、周回分割（純粋関数）
      ├─ lib/clock-svg.js   文字盤・針・扇形・リングの描画（SVG DOM）
      ├─ lib/animator.js    requestAnimationFrame による補間、速度、スクラブ
      ├─ lib/problems/*.js  パターン別の問題生成器（シード付き乱数）
      ├─ lib/print.js       A4シートの組版
      └─ style.css / print.css / tokens.css
          │
          ▼ 配信
Cloudflare Workers（src/worker.js）
      ├─ ASSETS（public/ を配信）
      └─ /api/*（Phase 2：履歴・苦手記録 → D1）
```

### 9.3 時計 SVG の設計

- `viewBox="0 0 200 200"`、中心 (100, 100)、文字盤半径 90。すべての描画をこの座標系で行い、CSSでサイズを変える。
- 角度は12時方向を 0°、時計回りを正とする。
  - 分針：`θm = m × 6°`
  - 時針：`θh = (h mod 12) × 30° + m × 0.5°`
  - 秒針（オプション）：`θs = s × 6°`
- 経過分 `Δ`（負なら前）を与えると、開始角から `Δ × 6°` 分の扇形を描く。`|Δ| > 60` の場合は 60分ごとに分割し、周回 k ごとに半径を `r - 6k` として重ねる。
- 扇形パスは `M 中心 L 開始点 A r r 0 大弧フラグ 回転方向 終了点 Z`。フルリングは2つの半円弧で描く。
- 針は `transform="rotate(θ 100 100)"` で回転させ、アニメーション中は角度のみ更新する（再描画コストを抑える）。
- 目盛りは 60本（5分ごとに太く）、数字 1〜12、オプションで分の数字 0〜55 を外周に表示。
- ドラッグ操作は Pointer Events で中心からの角度を求め、分の刻み設定に丸める。

### 9.4 アニメーション

- `requestAnimationFrame` で「経過分」を時間補間し、扇形・針・数直線・時間ブロックをすべて同じ値から描く（単一の状態から派生）。
- 速度：ゆっくり（10分/秒）、ふつう（30分/秒）、いっき（即時）。
- 12通過の検出は補間値の整数部の変化で行い、バッジ表示と時の数字更新を発火する。
- `prefers-reduced-motion` または設定「動きをへらす」では、段階表示（10分ごとにコマ送り）に切り替える。

### 9.5 問題生成

- 各パターンは `generate(rng, options) → Problem` の純粋関数。`rng` はシード付き（mulberry32 など）で、プリント再印刷と問題共有に使う。
- 4択の誤答は「繰り上がり忘れ（3時70分 → 3時10分）」「時針の読み違い（1つ前の時）」「向きの間違い（前後の逆）」から生成し、ランダムな数字を避ける。
- 生成後に `validate(problem)` で、答えが 0〜23時・0〜59分の範囲、難易度制約（刻み、上限）を満たすことを検査する。
- 直近の問題との重複を避ける（同一セット内で同じ出発時刻・移動量を出さない）。

### 9.6 Cloudflare 構成

| 項目 | 内容 |
|---|---|
| Worker | `src/worker.js`。`/api/*` 以外は `env.ASSETS.fetch`。算数筆算と同じ雛形 |
| 静的アセット | `public/` を `assets.directory` に指定。`run_worker_first: ["/api/*"]` |
| ドメイン | `tokei.molkz.com`（仮）。`routes` に `custom_domain: true` |
| D1（Phase 2） | 匿名IDのCookieで `history`、`weakpoints` を保存。マイグレーションは `migrations/` |
| PWA | `manifest.webmanifest` + `sw.js` で画面資産をキャッシュ。オフラインでも学習・プリント生成が可能 |
| ヘッダ | キャッシュ・セキュリティヘッダを Worker で付与 |

### 9.7 ディレクトリ構成

```
時計の読み方/
├── public/
│   ├── index.html
│   ├── app.js
│   ├── style.css / print.css / tokens.css
│   ├── lib/
│   │   ├── time.js
│   │   ├── angles.js
│   │   ├── clock-svg.js
│   │   ├── animator.js
│   │   ├── rng.js
│   │   ├── print.js
│   │   └── problems/
│   │       ├── index.js
│   │       ├── read.js          (P1, P2)
│   │       ├── after-before.js  (P3, P4, P5, P6)
│   │       ├── duration.js      (P7)
│   │       ├── ampm.js          (P8)
│   │       ├── units.js         (P9)
│   │       └── story.js         (P10)
│   ├── icons/
│   ├── manifest.webmanifest
│   └── sw.js
├── src/
│   ├── worker.js
│   ├── api/          (Phase 2)
│   └── lib/http.js
├── migrations/       (Phase 2)
├── tests/
│   ├── time.test.mjs
│   ├── angles.test.mjs
│   └── problems.test.mjs
├── docs/
│   ├── USAGE.md
│   └── screenshots/
├── wrangler.jsonc
├── package.json
├── README.md
└── 時計の読み方_開発計画書.md（本書）
```

### 9.8 テスト戦略

| 層 | 方法 | 対象 |
|---|---|---|
| 単体 | `node --test` | 時刻の加減（境界：0分、59分、11時、23時、午前午後跨ぎ、負の値）、角度、扇形分割、問題生成の制約と再現性 |
| UI | Playwright（evaluator エージェント） | 各モードの遷移、アニメーション完了後の答え表示、針ドラッグ、印刷プレビューのページ数、スマホ幅レイアウト |
| 印刷 | Chrome の PDF出力を目視確認 | A4 1枚に収まること、扇形がグレーで印刷されること、解答ページの対応 |
| 手動 | 子どもによる利用観察 | 説明なしで「まなぶ」を触れるか、解説アニメで納得できるか |

---

## 10. データモデル

### Problem（画面・印刷で共通）

```
{
  id: "P4-1234-3",          // パターン-シード-連番
  pattern: "P4",
  start: { h: 3, m: 10, ampm: "pm" | null },
  delta: 80,                // 分。負なら前。P6 は 60 の倍数
  end:   { h: 4, m: 30, ampm: "pm" | null },   // P7 では両端が与えられ delta が答え
  answerType: "time" | "minutes" | "hours" | "ampm" | "draw",
  text: "3時10分です。80分後は何時何分ですか？",
  choices: [ ... ],         // 4択のとき
  difficulty: 3,
  options: { step: 5, showMinuteNumbers: false }
}
```

### Settings（localStorage）

```
{ furigana: true, ampmLabel: "kanji" | "none", minuteNumbers: false,
  speed: "normal", reduceMotion: false, font: "klee" }
```

### Phase 2（D1）

- `history(id, uid, pattern, difficulty, seed, correct, total, created_at)`
- `weakpoints(id, uid, pattern, tag, count, updated_at)`：tag は「繰り上がり忘れ」「向きの間違い」など誤答分類

---

## 11. 開発スプリント計画

| Sprint | ゴール | 主な成果物 | 完了条件 |
|---|---|---|---|
| **0** 足場 | ローカルで空の画面が動く | `wrangler.jsonc`、`package.json`、`worker.js`、`index.html`、tokens 流用、ハッシュルーター、README 雛形 | `npm start` で S01 が表示される |
| **1** 時計エンジン | 時計が描け、動く | `time.js`、`angles.js`、`clock-svg.js`、`animator.js`、単体テスト、**まなぶ**（S02）の +/− ボタンと扇形・周回リング・繰り上がりバッジ | 「3時10分 + 80分」が第4.2章のシナリオ通りに再生される。テスト通過 |
| **2** 量の表現と操作 | 数直線・時間ブロック・針ドラッグ | 数直線、時間ブロック、スクラブ、速度切替、針ドラッグ、reduced-motion | まなぶモードが受け入れ基準 A を満たす |
| **3** れんしゅう | 出題と解説ができる | `rng.js`、`problems/`（P1〜P5）、S03〜S06、4択・針合わせ・数入力、解説アニメーション | 10問セットを通しで解ける。問題生成のテスト通過 |
| **4** プリント | A4テストを印刷できる | `print.js`、`print.css`、S07〜S08、解答ページ、シードURL | 6 / 8 / 10 問でA4 1枚に収まり、解答ページが対応する |
| **5** 残りパターンと設定 | 全パターンと設定 | P6〜P10、難易度プリセット、S09、ふりがな、フォント切替、PWA | 受け入れ基準 B〜D を満たす |
| **6** 公開 | 専用ドメインで公開 | デプロイ、スマホ対応の仕上げ、Playwright 検証、README・USAGE・スクリーンショット | `tokei.molkz.com`（仮）で全モードが動く |
| **Phase 2** | 記録と苦手対策 | D1、`/api/history`、`/api/weakpoints`、きろく画面、苦手パターンの優先出題、秒針・時間同士の計算 | 別途計画 |

Sprint 1〜2 が本アプリの価値の中核なので、ここで子どもに触ってもらい、視覚化の分かりやすさを確認してから Sprint 3 以降に進む。

---

## 12. 受け入れ基準

### A. まなぶ（視覚化）

- [ ] 任意の時刻から +5 / +10 / +30 / +60 分、−5 / −10 / −30 / −60 分に針が動き、扇形が動いた範囲に一致する。
- [ ] 60分を超える移動で、1周目がフルリング、2周目以降が内側の扇形として区別して表示される。
- [ ] 分針が12を通過した瞬間に「+1時間」（逆向きは「−1時間」）が表示され、時の数字が変わる。
- [ ] 数直線と時間ブロックが扇形と同じ量・同じ色で表示され、スクラブに連動する。
- [ ] 「80分 = 1時間20分」のような分解が時間ブロックと式の両方で表示される。
- [ ] 時針が分針と連動して滑らかに動く（3時30分で時針が3と4の中間にある）。
- [ ] 針をドラッグして時刻を合わせられ、分の刻み設定に丸められる。
- [ ] 動きを減らす設定で、コマ送り表示に切り替わる。

### B. れんしゅう

- [ ] P1〜P10 の各パターンで、難易度の制約を満たす問題が生成される。
- [ ] 4択の誤答が典型的な誤り（繰り上がり忘れ等）から作られている。
- [ ] 誤答後、必ずアニメーション解説が再生され、再挑戦せずに次へ進める。
- [ ] 10問セットの結果画面から、間違えた問題の解説を再生できる。
- [ ] 問題文を読み上げできる（Web Speech API。未対応環境ではボタンを隠す）。

### C. プリント

- [ ] 6 / 8 / 10 問がA4縦1枚に収まり、ページ境界で問題が切れない。
- [ ] 問題ページと解答ページの番号・内容が対応する。
- [ ] 解答ページで動いた範囲がグレーの扇形として印刷される。
- [ ] 同じシードのURLを開くと同じプリントが再生成される。
- [ ] スマホでプレビューが縮小表示され、印刷ボタンが画面下部に固定される。

### D. 共通・アクセシビリティ

- [ ] タッチ対象は 44px 以上。針のドラッグはタブレットで操作できる。
- [ ] 色だけに頼らず、方向は矢印と線種でも区別される。
- [ ] ふりがな表示の切替ができ、小2配当外の漢字を使わない。
- [ ] キーボードだけで主要操作ができる（ボタン、選択肢、+/−）。
- [ ] 初回表示が 1 秒以内（画面資産の合計 300KB 以下、外部ライブラリなし）。
- [ ] オフラインでもまなぶ・れんしゅう・プリント生成が動く。

---

## 13. 子ども向け・アクセシビリティの配慮

- 誤答表示に大きな×や警告音を使わず、「もういちど見てみよう」と再生ボタンを出す。
- 文字は大きく、時計の数字は文字盤の外周にゆとりを持って配置する。
- 午前・午後は「午前 / 午後」の表記に加え、24時間の帯（太陽・月のアイコン）で示す。
- 音声読み上げは任意で、初期状態ではオフ。
- 画面内に外部リンク、広告、SNS 共有を置かない（保護者向けの README・GitHub リンクは設定画面のみ）。

---

## 14. リスクと対策

| リスク | 対策 |
|---|---|
| 扇形・リング・数直線・ブロックを同時に出すと画面が煩雑になる | 初期表示は扇形のみとし、数直線・ブロックは「くわしく」で展開。子どもの観察で要素数を調整する |
| 周回が3周を超える（180分超）と年輪が潰れる | 移動量の上限を 180 分に固定。それ以上は P6（〜時間後）として時針中心に見せる |
| 針ドラッグがタッチで細かく操作しづらい | 刻み設定で丸める。ドラッグ中は針を拡大表示し、時刻を大きく表示する |
| 印刷時に扇形の色が抜ける | `print-color-adjust: exact` を指定し、解答ページの扇形はグレー＋斜線パターンにする |
| Web Speech API の日本語音声が環境依存 | 補助機能に留め、未対応時は非表示。文章は短く、ふりがなで補う |
| Safari の SVG `transform` 挙動差 | `transform` 属性（CSS ではなく）を使い、iPad Safari で Sprint 1 時点から確認する |

---

## 15. 未決事項（確認したいこと）

1. **アプリ名とドメイン**：仮に「時計の読み方」「tokei.molkz.com」としている。名称（例：「とけいの学習ラボ」など）とサブドメインを決めたい。
2. **履歴保存（D1）の時期**：本書では Phase 2 としている。初期リリースに含める必要があれば Sprint 3 に前倒しする。
3. **24時間表記（15:10）と秒**：小2の範囲外のため発展扱い（オプション）としている。不要なら外す。
4. **音声読み上げ**：Web Speech API を任意機能として入れる想定。不要なら外す。
5. **姉妹アプリとのデザイン統一**：tokens.css を流用し、印刷レイアウトも揃える想定。子ども向けにもう少し柔らかいトーン（配色・角丸）にするかどうか。
6. **回答方式の初期値**：れんしゅうモードの初期は「4択」を想定。針合わせを主にするか。
7. **GitHub リポジトリ**：姉妹アプリと同様に独立リポジトリ（例：`Satoshi-Hiramatsu/tokei-app`）に分離するか。

未決事項は、Sprint 0 着手前に決まっていなくても、本書の仮置きで進められる。
